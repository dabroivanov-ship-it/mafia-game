# Copy to server/.env on the VPS (never commit server/.env)
NODE_ENV=production
PORT=3001

# Required — min 32 random characters (openssl rand -hex 32)
JWT_SECRET=

# Comma-separated admin usernames (promoted on server start if account exists)
ADMIN_USERNAMES=admin

# Required in production — your public site URL (no trailing slash)
CORS_ORIGIN=https://realmafia.online
SITE_URL=https://realmafia.online

# Set on VPS behind nginx/reverse proxy so rate limits use real client IP
TRUST_PROXY=1

# Optional: Telegram bot token for Web App and /start handler
# TELEGRAM_BOT_TOKEN=1234567890:AA...
# TELEGRAM_WEBAPP_URL=https://realmafia.online
# Chat ID for backup delivery (can also be set in admin → Backups)
# TELEGRAM_BACKUP_CHAT_ID=123456789

# Telegram OIDC login (BotFather → Bot Settings → Web Login → OpenID Connect)
# TELEGRAM_OIDC_CLIENT_ID=1234567890
# TELEGRAM_OIDC_CLIENT_SECRET=your-client-secret-from-botfather
# Optional override (default: {CORS_ORIGIN}/api/auth/telegram/oidc/callback)
# TELEGRAM_OIDC_REDIRECT_URI=https://realmafia.online/api/auth/telegram/oidc/callback

# VK ID login (https://id.vk.com — web app + trusted redirect URI)
# VK_CLIENT_ID=1234567
# VK_CLIENT_SECRET=your-service-token-from-vk-id
# Optional override (default: {CORS_ORIGIN}/api/auth/vk/callback)
# VK_REDIRECT_URI=https://realmafia.online/api/auth/vk/callback

# Local dev only: skip JWT/CORS hard checks (never use on VPS)
# ALLOW_INSECURE_DEV=1

# Optional overrides (defaults: server/data/mafia.db, server/uploads/avatars)
# DB_PATH=/home/mafia-game/server/data/mafia.db
# UPLOADS_DIR=/home/mafia-game/server/uploads/avatars
